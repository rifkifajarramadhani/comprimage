# Copy to .env. Optional overrides for docker compose.
# Host port the dev server (web-dev, `--profile dev`) is published on.
FRONTEND_DEV_PORT=3000
# Image tag pulled/built for the prod service (web-prod, `--profile prod`).
IMAGE_TAG=latest
